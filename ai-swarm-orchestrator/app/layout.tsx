import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Providers } from './providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'AI Swarm Orchestrator | Enterprise Edition',
  description: 'Enterprise-grade AI agent orchestration platform for autonomous software development',
  keywords: ['AI', 'agents', 'automation', 'orchestration', 'software development'],
  authors: [{ name: 'US-SPURS' }],
  themeColor: '#0ea5e9',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="dark">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
}
